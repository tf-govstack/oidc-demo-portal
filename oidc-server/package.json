{
  "name": "oidc-server",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "SET PORT=8888 && SET IDP_BASE_URL=http://localhost:8088/v1/idp && node app.js",
    "devstart": "SET PORT=8888 && SET IDP_BASE_URL=http://localhost:8088/v1/idp && node app.js && nodemon ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.1.3",
    "express": "^4.18.2",
    "joi": "^17.7.0",
    "nodemon": "^2.0.20"
  }
}